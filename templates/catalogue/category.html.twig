{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12 text-center mb-4">
            <h1 class="fs-1">Nos Categories :</h1>
            {# Titre centré avec une marge inférieure (mb-4) et une taille de police large (fs-1) pour le texte #}
        </div>
    </div>

    <div class="row justify-content-center">
        {# Début d'une rangée Bootstrap qui centre les colonnes #}
        
        {% for categorie in category %}
        <div class="col-12 col-sm-6 col-md-4 mt-3 d-flex">
            {# Chaque catégorie est contenue dans une colonne responsive : 
                - col-12 : occupe 100% de la largeur sur mobile,
                - col-sm-6 : occupe 50% de la largeur sur les petits écrans,
                - col-md-4 : occupe 33% sur les écrans moyens.
               La classe mt-3 ajoute une marge supérieure, et d-flex permet à la carte de s'adapter à la hauteur. #}
            
            <div class="card w-100">
                {# La carte occupe toute la largeur disponible avec w-100 #}
                <a class="text-decoration-none" href="{{ path('app_dishincat', {categorie_id: categorie.id}) }}">
                    {# Le lien autour de la carte est sans soulignement grâce à text-decoration-none #}

                    <img src="{{ asset('img/category/' ~ categorie.image) }}" 
                         class="card-img-top imagecat" 
                         alt="{{ categorie.libelle }}" 
                         style="max-height: 200px; width: 100%; object-fit: cover;">
                    {# L'image est ajustée pour s'adapter correctement :
                        - max-height: 200px limite la hauteur,
                        - width: 100% assure que l'image prend toute la largeur de la carte,
                        - object-fit: cover permet de couper l'image de manière esthétique sans la déformer. #}

                    <div class="card-body text-center">
                        {# Corps de la carte avec le texte centré #}
                        <h5 class="card-title">{{ categorie.libelle | upper }}</h5>
                        {# Le titre de la carte affiche le libellé de la catégorie en majuscules #}
                    </div>
                </a>
            </div>
        </div>
        {% endfor %}
        {# Fin de la boucle qui affiche toutes les catégories sous forme de cartes #}
        
    </div>
</div>
{% endblock %}
